# Vercel-optimized requirements
# Minimal dependencies for faster cold starts

Flask==3.0.0
Flask-Session==0.5.0
google-auth==2.25.2
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
google-api-python-client==2.111.0
Werkzeug==3.0.1

# HTML Parsing
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Cloud Vision API for OCR (serverless-compatible)
google-cloud-vision==3.4.5

# Image processing (lightweight)
Pillow>=10.0.0
requests>=2.31.0

# Caching (for better session management on serverless)
cachelib==0.10.2
